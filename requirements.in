# Runtime dependency inputs. The resolved, reviewable lock is requirements.txt.
# The versions below are the verified CPython 3.12.13 baseline. The project
# still declares Python >=3.10, but this lock is not claimed to be a universal
# Python/platform artifact lock; regenerate it for another interpreter or OS.
curl_cffi==0.16.0
pyotp==2.10.0
flask==3.1.3
psycopg[binary,pool]==3.3.4
requests==2.32.3
pycryptodome==3.23.0
selenium==4.47.0
websocket-client==1.8.0
cryptography==48.0.1
cloakbrowser[geoip]==0.5.7
playwright==1.62.0
python-dotenv==1.2.3
